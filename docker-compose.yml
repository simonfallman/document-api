services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      # Persist SQLite DB, ChromaDB, and uploaded documents across restarts
      - ./data:/app/data
    restart: unless-stopped
